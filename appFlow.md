```
<App></App>
    <Header>
    <Map>
        <GoogleMap>
            <Marker>
    <Button>
        <button>
    <Gallery-Container>
        <Gallery>
            <ul>
                <li> * 15
```

1. First Load ->  Request issPosition -> location = {latitude, longitude} -> this.state { location } -> Map { center }